{
  "name": "basic-sample",
  "version": "1.0.0",
  "description": "basic-sample: An Ionic project",
  "engines": {
    "npm": "2.0.2",
    "node": "0.10.36"
  },
  "dependencies": {
    "gulp": "^3.5.6",
    "gulp-sass": "^0.7.1",
    "gulp-concat": "^2.2.0",
    "gulp-minify-css": "^0.3.0",
    "gulp-rename": "^1.2.0"
  },
  "devDependencies": {
    "bower": "^1.3.3",
    "gulp-util": "^2.2.14",
    "shelljs": "^0.3.0"
  },
    "scripts": {
    "postinstall": "npm update -g && ionic serve -s -c --lab",
    "test": "grunt test -v"
  },
  "cordovaPlugins": [
    "org.apache.cordova.inappbrowser"
  ]
}
